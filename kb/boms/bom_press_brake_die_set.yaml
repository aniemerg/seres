id: bom_press_brake_die_set
kind: bom
owner_item_id: press_brake_die_set
components:
  - item_id: tool_steel_high_carbon_v0
    qty: 40.0
    unit: kg
notes: Minimal BOM for a general-purpose press brake tooling set. Uses high-carbon
  tool steel so the recipe can preserve hardening, tempering, precision grinding,
  and inspection requirements; specialized bend geometries should be split only
  when a process explicitly needs them.
