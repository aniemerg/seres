id: heliostat_array_assembly_v0
kind: process
name: Heliostat unit assembly (legacy id)
process_type: batch
is_template: true
inputs: []
outputs:
- item_id: heliostat_unit_v0
  qty: 1.0
  unit: unit
resource_requirements:
- machine_id: labor_bot_general_v0
  qty: 1
  unit: count
- machine_id: assembly_tools_basic
  qty: 1.0
  unit: count
energy_model:
  type: fixed_per_batch
  value: 5.0
  unit: kWh
time_model:
  type: batch
  hr_per_batch: 1.0
notes: Legacy process id retained for compatibility; assembles one heliostat_unit_v0.
  Model fields or arrays as multiple heliostat units by quantity rather than a
  separate array-system machine.
